# Copy to frontend/.env
PORT=3000
REACT_APP_API_BASE_URL=http://localhost:8000/api/v1
# Your Google Maps JavaScript API key (Maps + Marker). Leave empty to see a
# graceful placeholder instead of the live map.
REACT_APP_GOOGLE_MAPS_API_KEY=
